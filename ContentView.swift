import SwiftUI
struct ContentView: View {
    var body: some View {
        VStack {
            Text("Hello IPAGURU!")
                .font(.largeTitle)
                .bold()
            Text("Your app works 🎉")
        }
    }
}